<% if userLogedIn && @idea.user_id != @user.id %>
	<% if @user.hasInFavorites(@idea) %>
		<%= link_to "Remove from favorites", removeFromFavorites_path(@idea.id), :remote => true %>
	<% else %>
		<%= link_to "Add to favorites", addToFavorites_path(@idea.id), :remote => true %>
	<% end %>
<% end %>