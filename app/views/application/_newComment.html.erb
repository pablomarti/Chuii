<%= form_for :comment, :url => {:controller => "comments", :action => "newComment", :item => item, :id => id}, :remote => true do |f| %>
	<%= f.text_area :comment, :placeholder => "Here goes your comment", :maxlength => 400, :class => "shorText" %><br />
	<%= f.submit :value => "Save it" %>
<% end %>